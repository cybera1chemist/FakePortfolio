# 第 1 步：搭建项目骨架（你来动手）

## 1.1 先理解：静态网站是怎么工作的？

浏览器打开一个网页时，过程是这样的：

1. 向服务器请求一个 HTML 文件，服务器 原样返回文件内容 （没有任何后端程序运行，所以叫"静态"）；
2. 浏览器 从上到下解析 HTML ，遇到`<link>` 、`<script>` 、`<img>` 标签时，再按标签里的路径分别去请求 CSS、JS、图片；
3. 所以 文件在文件夹里的位置，直接决定它的网址（URL）路径 。这就是为什么第一步要先把目录规划好。

## 1.2 目标目录结构

请你在`FakePortfolio` 文件夹里手动创建下面的结构（用 VS Code 或文件资源管理器都行）：



```
FakePortfolio/
 ├── index.html ← 主页（服务器默认会找这个文件名）
 ├── works/ ← 5 个作品详情页放这个文件夹里 │ ├── work-1.html │ ├── work-2.html │ ├── work-3.html │ ├── work-4.html │ └── work-5.html └── assets/ ← 所有"资源文件"集中放这里 ├── css/ │ └── style.css ← 6 个页面共享的同一份样式表 ├── js/ │ └── main.js └── img/ ← 头像和作品图片（现在先空着）
```

这里有两个行业惯例值得记住：

- 文件名全部用英文、小写、单词间用短横线 （如`work-1.html` ）。不要用中文或空格——URL 里的中文和空格会被转义成`%E4%BD%A0...` 这种编码，既难看又容易在部署后出现图片加载不出来的经典 bug。
- CSS 单独成文件而不是写在每个页面的`<style>` 里：6 个页面 共享一份`style.css` ，浏览器下载一次就会缓存，改风格也只改一处。

## 1.3 你的动手任务

任务 A：创建`index.html` ，要求自己默写 HTML5 骨架，必须包含：

- 第一行的文档类型声明（提示：`<!DOCTYPE ...>` ，作用是告诉浏览器用现代标准模式解析，而不是兼容远古 IE 的怪异模式）
- `<html>` 标签上的`lang` 属性，值设为`zh-CN` （想想为什么：屏幕阅读器要靠它决定用什么语言朗读，搜索引擎也靠它判断语言）
- `<head>` 里依次有：字符编码声明（UTF-8， 必须放在最前面 ，否则页面可能乱码）、移动端适配必需的那个`meta viewport` 标签、`<title>` 、用`<link>` 引入`assets/css/style.css`
- `<body>` 里先随便写一个`<h1>` ，内容比如"我的作品集"，纯粹用于验证页面能打开
  任务 B：创建空的`assets/css/style.css` ，在里面写一条测试样式，比如给`body` 设置一个明亮的背景色（先感受一下多巴胺配色，试试`#FFF4D6` 奶油黄），用来确认 CSS 文件确实被成功引入了。

任务 C：5 个详情页文件先创建为空文件即可 ，第 5 步我们再填内容。

## 1.4 验证方式

直接双击`index.html` 用浏览器打开：能看到带奶油黄背景的标题，就说明 HTML 骨架和 CSS 引入路径都正确。
补充：双击打开用的是`file://` 协议。以后引入字体、用 JS 请求数据时，`file://` 会有安全限制。到时我会教你用本地预览服务器（VS Code 的 Live Server 插件，或一行命令`python -m http.server` ），现在双击足够。

## 1.5 做完后回答我两个思考题（这是本步的核心知识点）

1. 主页在根目录，引用 CSS 写的是`assets/css/style.css` 。那么 `works/work-1.html` 里如果要引用同一份 CSS，`href` 应该写成什么？ 为什么要多个`../` ？
2. 如果作品图片`cat.png` 放在`assets/img/` 里，`work-1.html` 中`<img>` 的`src` 该怎么写？

